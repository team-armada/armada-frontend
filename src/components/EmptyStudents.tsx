import { Link } from 'react-router-dom';
import { Button, Flex } from '@chakra-ui/react';

const EmptyStudents = () => {
  return <p>There are no students currently taking this course.</p>;
};

export default EmptyStudents;
